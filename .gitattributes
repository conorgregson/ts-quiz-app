# Normalize text files (line endings) safely
* text=auto

# Common text types
*.ts   text
*.tsx  text
*.js   text
*.mjs  text
*.cjs  text
*.html text
*.css  text
*.json text
*.md   text
*.yml  text
*.yaml text

# Images/binaries must NOT be treated as text
*.png  binary -text
*.jpg  binary -text
*.jpeg binary -text
*.gif  binary -text
*.webp binary -text

# SVG is text (diffable)
*.svg  text
